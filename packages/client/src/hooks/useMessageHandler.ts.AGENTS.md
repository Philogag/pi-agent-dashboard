# useMessageHandler.ts — index

New `case "view_messages_update"`: replaces `viewMessagesMap.get(sessionId)` with `msg.viewMessages.slice()`. New setter `setViewMessagesMap` in `MessageHandlerSetters`. See change: render-file-previews. Dispatches every `ServerToBrowserMessage` to the right state setter. New switch cases `worktree_init_progress` / `worktree_init_done` / `worktree_init_failed` forward into `dispatchInitEvent` (the worktree-init bus). `spawn_error` case enhanced: after writing `spawnErrors`, calls `isVisibleCwd(...)` against `deps.cwdVisibilityInputsRef?.current`; when off-screen, calls `pushSpawnErrorToast({cwd, code, message})`. New optional dep `cwdVisibilityInputsRef` (back-compat: omitted → no fallback toast). See change: harden-worktree-spawn. Handles favorite_models_updated → setFavoriteModels; setter added to MessageHandlerSetters. See change: enrich-model-selector-capabilities-favorites. `case "plugin_event"` routes broadcast into plugin per-session store via `publishSessionEvent(sessionId, event)` so `useSessionEvents` consumers (goal-plugin `GoalChip`) re-derive. See change: add-goal-continuation-plugin. New setter `setLoadingHistory` + new dep `loadingHistoryTimersRef`. `case "event_replay"` clears loading flag via `clearLoadingHistory` when `events.length>0` (first content) OR `isLast===true` (empty terminal). `session_updated` clears flag when `updates.dataUnavailable===true`. See change: show-chat-history-loading-indicator. `case "session_added"`: Tier 1 / Tier 2 / Tier 2.5 correlation+navigate cascade wrapped in `if (!msg.session.hidden)`. Hidden sessions (auto-hidden headless workers — subagent, `memory` tool, nested `pi -p` — share parent cwd) excluded from auto-navigation AND correlation consumption: no `pendingSpawns` delete, no `clearSpawningCwd`, no navigate. `setSessions` map update stays unconditional; hidden card still renders in Hidden tier. See change: suppress-hidden-session-auto-navigation. Adds setter `MessageHandlerSetters.setFolderGitMap`; handles `git_head_update` upserting `folderGitMap(cwd→branch\|null)`. See change: refresh-folder-header-branch. New `case "prompt_received"` dispatches `applyPromptReceived` on session state. See change: optimistic-prompt-progress. deps.replayPersister records event/event_replay into durable replay buffer (seed on reset, record on append), drop on session_state_reset. See change: reduce-session-replay-traffic. Handles `file_changed` → `setChangedOnDisk`. See change: split-editor-workspace. `terminal_added`: adds to `terminals` map + clears `pendingTerminalCwdRef` / records `lastCreatedTerminalIdRef`, but NO LONGER navigates (removed `/folder/:cwd/terminals` route deselected the session); the pane's terminal slice opens the `term:<id>` tab in-place. See change: terminals-in-tabbed-panes. `case "workspaces_updated"` also calls `setWorkspacesLoaded(true)`; new setter `setWorkspacesLoaded` in `MessageHandlerSetters` gates DirectoryHomeView's cold-load guard for workspace-only cwds. See change: enable-workspace-folder-home-page. New `case "notify"` → `addNotify` (render-only row, never `addInteractiveRequest`). See change: split-notify-from-prompt-request. Owns the second replay flag `replayInFlight` (`setReplayInFlight` + `replayInFlightTimersRef` deps): `event_replay` clears it ONLY on `isLast === true` and re-arms `HYDRATE_CEILING_MS` on EVERY non-terminal batch (content batches included — a batch on the wire is the liveness signal); the re-arm touches the timers ref only, never the setter, so a multi-batch replay costs no extra render. `session_updated { dataUnavailable: true }` clears it alongside `loadingHistory`. See change: show-replay-in-flight-indicator. New `case "session_orphaned"`: toasts `session.orphanedProcess` (error) naming the surviving pid; arrives immediately BEFORE `session_removed`, because the server releases the record even when the process outlived SIGTERM → SIGKILL — without it the user sees an ordinary successful close while a ~127 MB `pi` stays resident. See change: fix-tmux-session-shutdown-leak. Reset/replay carry sites go through `carryPendingPrompt` — a `sending` bubble is never restored. See change: fix-optimistic-prompt-stuck-sending. `models_list` also drives `setModelRefreshErrorsMap` (per-session, non-sticky: a later clean push deletes the session's entry). See change: upgrade-model-selector-primitives. `session_removed` marks reducer state ended and clears `retryState`/`lastError`/`retryCancelled` while preserving messages/stats; raw reconnect snapshots do not clear lifecycle state. See change: fix-retry-error-lifecycle.

New `case "history_window"` (stores per-session `HistoryGapState`; `gapCount: 0` clears it) and `case "history_backfill_result"` (splices the segment into `messages[]` immediately above the `HISTORY_GAP_ROW_ID` row, reducing it from a FRESH state). Splice touches `messages[]` ONLY — no `maxSeqMapRef` move, no `publishSessionEvents`, no `replayPersister` write. The `event_replay` fold places the divider row at the head→tail boundary (first event with `seq >= tailMinSeq`). `replayPersister` seed/record SUPPRESSED for any session whose replay carried a non-zero `gapCount`, else a sparse array caches as contiguous and the next reload delta-subscribes with the gap permanently invisible. Backfill armed only on the terminal batch (`isLast: true`); gap dropped on `session_state_reset`. Optional setter `setHistoryGaps`. See change: lazy-load-session-history (D5, D10, D11, D12).
